# branch is X commits behind master

```
git checkout master       # you are switching your branch to master
git pull                  # make sure it's up to date
git checkout devel        # switch back to your devel branch
git merge master          # merge changes 
git push                  # pushes updated devel branch back to remote
```
